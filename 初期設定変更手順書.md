# 🔧 物販管理システム 初期設定変更手順書

このドキュメントでは、システムを導入する際に**変更が必要な箇所**をすべて記載しています。  
Gitからコードを取得後、以下の手順に従って設定を変更してください。

---

## 📋 変更箇所一覧

| No. | ファイル | 変更内容 | 重要度 |
|-----|----------|----------|--------|
| 1 | `app.py` | 管理者アカウント情報（PostgreSQL用） | ⚠️ 必須 |
| 2 | `app.py` | 管理者アカウント情報（SQLite用） | ⚠️ 必須 |
| 3 | `営業資料.md` | 連絡先情報 | 任意 |
| 4 | `営業資料.html` | 連絡先情報 | 任意 |
| 5 | `render.yaml` | サービス名 | 任意 |

---

## ⚠️ 1. 管理者アカウント情報の変更（必須）

### 変更ファイル: `app.py`

セキュリティ上、**初期パスワードは必ず変更してください**。

---

### 📍 変更箇所①：PostgreSQL用（195行目付近）

**検索する文字列:**
```python
''', ('admin', 'admin@example.com', generate_password_hash('admin123'), 'admin', '管理者'))
```

**変更後の例:**
```python
''', ('admin', 'your-email@your-domain.com', generate_password_hash('安全なパスワード'), 'admin', '管理者'))
```

---

### 📍 変更箇所②：SQLite用（343行目付近）

**検索する文字列:**
```python
''', ('admin', 'admin@example.com', generate_password_hash('admin123'), 'admin', '管理者'))
```

**変更後の例:**
```python
''', ('admin', 'your-email@your-domain.com', generate_password_hash('安全なパスワード'), 'admin', '管理者'))
```

---

### 🔍 変更手順（VSCode / メモ帳）

1. `app.py` をテキストエディタで開く
2. `Ctrl + F`（検索）で `admin@example.com` を検索
3. 2箇所見つかるので、それぞれ以下を変更：
   - `admin@example.com` → 実際のメールアドレス
   - `admin123` → 安全なパスワード（8文字以上推奨）
4. ファイルを保存

---

### 💡 変更するパラメータ一覧

| パラメータ | 現在の値 | 変更内容 |
|------------|----------|----------|
| ユーザー名 | `admin` | 変更不要（または任意の名前） |
| メールアドレス | `admin@example.com` | 管理者の実際のメールアドレス |
| パスワード | `admin123` | **必ず変更**（英数字8文字以上推奨） |
| 権限 | `admin` | 変更不要 |
| 表示名 | `管理者` | 任意の表示名 |

---

## 📧 2. 営業資料の連絡先変更（任意）

営業資料を使用する場合のみ変更してください。

---

### 📍 変更ファイル①: `営業資料.md`（136〜138行目）

**現在の内容:**
```markdown
- 📧 メール: info@example.com
- 📞 電話: 03-XXXX-XXXX
- 🌐 Web: https://example.com
```

**変更後の例:**
```markdown
- 📧 メール: contact@your-company.com
- 📞 電話: 03-1234-5678
- 🌐 Web: https://your-company.com
```

---

### 📍 変更ファイル②: `営業資料.html`

HTMLファイルにも同様の連絡先が含まれています。  
テキストエディタで開いて同様に変更してください。

---

### 📍 コピーライト表記（142行目付近）

**現在の内容:**
```markdown
© 2024 物販管理システム All Rights Reserved.
```

**変更後の例:**
```markdown
© 2024 株式会社〇〇 All Rights Reserved.
```

---

## 🌐 3. Renderサービス名の変更（任意）

デプロイ先のサービス名を変更したい場合のみ変更してください。

---

### 📍 変更ファイル: `render.yaml`（3行目）

**現在の内容:**
```yaml
services:
  - type: web
    name: merchandise-management
```

**変更後の例:**
```yaml
services:
  - type: web
    name: your-company-merchandise
```

> ⚠️ サービス名は**英数字とハイフンのみ**使用可能です。

---

## ⚙️ 4. 環境変数の設定（Render.com 完結型）

以下はコード変更不要で、Renderの管理画面から設定します。  
**Render内でPostgreSQLデータベースも作成するため、外部サービス（Supabase等）は不要です。**

> 📄 **詳細な手順は `環境変数設定手順書.xlsx` を参照してください**

| 環境変数 | 説明 | 設定方法 |
|----------|------|----------|
| `SECRET_KEY` | セッション暗号化キー | Renderで自動生成されます |
| `DATABASE_URL` | PostgreSQL接続文字列 | Render PostgreSQLから自動設定 |

### 📊 環境変数設定手順書（Excel）の内容

| シート名 | 内容 |
|----------|------|
| 1_Render_PostgreSQL作成 | データベース作成手順（全6ステップ） |
| 2_Render_Webサービス作成 | デプロイと環境変数設定（全10ステップ） |
| 3_環境変数一覧 | 各変数の詳細説明とセキュリティ注意事項 |
| 4_トラブルシューティング | よくあるエラーと解決方法 |

### 💰 Render PostgreSQL 料金

| プラン | 料金 | 容量 | 特徴 |
|--------|------|------|------|
| Starter | $7/月 | 1GB | 小規模〜中規模向け |
| Standard | $20/月 | 16GB | 本格運用向け |

> ⚠️ Render PostgreSQLは無料プランがありません（$7/月〜）

---

## ✅ 変更後のチェックリスト

デプロイ前に以下を確認してください：

- [ ] `app.py` の管理者メールアドレスを変更した
- [ ] `app.py` の管理者パスワードを変更した（2箇所）
- [ ] 営業資料を使う場合、連絡先を変更した
- [ ] 必要に応じてサービス名を変更した
- [ ] すべてのファイルを保存した
- [ ] GitHubにプッシュした

---

## 🚀 Renderへの更新手順

コードを変更した後、以下の手順でRenderに反映します。

---

### 方法1: GitHubプッシュで自動更新（推奨）

Renderは自動デプロイが有効になっている場合、GitHubへのプッシュを検知して自動的に更新されます。

#### 手順

```bash
# 1. 変更をステージング
git add .

# 2. コミット
git commit -m "設定を変更"

# 3. GitHubにプッシュ
git push origin main
```

#### 確認方法

1. [Render.com](https://render.com) にログイン
2. 対象のWeb Serviceをクリック
3. 「Events」タブでデプロイ状況を確認
4. 「Deploy succeeded」と表示されれば完了

---

### 方法2: Renderダッシュボードから手動更新

自動デプロイが無効の場合、または即座に更新したい場合。

#### 手順

1. [Render.com](https://render.com) にログイン
2. 対象のWeb Serviceをクリック
3. 右上の「Manual Deploy」ボタンをクリック
4. 「Deploy latest commit」を選択
5. デプロイが開始される（5〜10分）

---

### 📊 デプロイ状況の確認

| ステータス | 意味 |
|------------|------|
| 🔵 In progress | デプロイ中 |
| 🟢 Deploy succeeded | 成功 |
| 🔴 Deploy failed | 失敗（ログを確認） |

---

### ⚠️ デプロイ失敗時の対処

1. 「Logs」タブをクリックしてエラーを確認
2. よくある原因：
   - `requirements.txt` に不足しているパッケージ
   - Pythonの構文エラー
   - 環境変数の設定漏れ
3. エラーを修正して再度プッシュ

---

### 💡 初回デプロイ後の確認事項

1. URLにアクセスしてログイン画面が表示されるか
2. 設定した管理者アカウントでログインできるか
3. 商品登録・顧客登録が正常に動作するか

---

## ❓ よくある質問

**Q. パスワードを忘れた場合は？**  
A. データベースを直接操作するか、`app.py`のパスワードを変更して再デプロイしてください。

**Q. 複数の管理者を作りたい場合は？**  
A. システムにログイン後、管理者メニューからユーザーを追加できます。

**Q. 変更せずにデプロイした場合は？**  
A. 初回ログイン後、プロフィール画面からパスワードを変更してください。

---

## 📞 サポート

ご不明な点がございましたら、お気軽にお問い合わせください。

---

最終更新: 2024年12月

