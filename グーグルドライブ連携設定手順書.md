# Google ドライブ連携 設定手順書

商品登録時にGoogleドライブから直接画像を選択できる機能の設定手順です。

## 1. Google Cloud Consoleでプロジェクト作成

1. [Google Cloud Console](https://console.cloud.google.com/) にアクセス
2. 右上の「プロジェクトを選択」→「新しいプロジェクト」
3. プロジェクト名を入力（例: `merchandise-management`）
4. 「作成」をクリック

## 2. Google Picker APIを有効化

1. 左メニュー「APIとサービス」→「ライブラリ」
2. 「Google Picker API」を検索
3. 「Google Picker API」をクリック
4. 「有効にする」をクリック

## 3. Google Drive APIを有効化

1. 左メニュー「APIとサービス」→「ライブラリ」
2. 「Google Drive API」を検索
3. 「Google Drive API」をクリック
4. 「有効にする」をクリック

## 4. OAuth同意画面の設定

1. 左メニュー「APIとサービス」→「OAuth同意画面」
2. User Type: 「外部」を選択 →「作成」
3. 以下を入力:
   - アプリ名: `物販管理システム`
   - ユーザーサポートメール: 自分のメールアドレス
   - デベロッパーの連絡先: 自分のメールアドレス
4. 「保存して次へ」
5. スコープの追加:
   - `https://www.googleapis.com/auth/drive.readonly`
6. 「保存して次へ」→「ダッシュボードに戻る」

## 5. OAuth 2.0クライアントIDの作成

1. 左メニュー「APIとサービス」→「認証情報」
2. 「+ 認証情報を作成」→「OAuth クライアント ID」
3. アプリケーションの種類: 「ウェブアプリケーション」
4. 名前: `merchandise-web-client`
5. 承認済みの JavaScript 生成元に追加:
   - `http://localhost:5000` （ローカル開発用）
   - `https://あなたのドメイン.onrender.com` （本番用）
6. 「作成」をクリック
7. **クライアントID**をメモ（後で使用）

## 6. APIキーの作成

1. 「+ 認証情報を作成」→「APIキー」
2. 作成されたAPIキーをコピー
3. 「キーを制限」をクリック（推奨）:
   - アプリケーションの制限: 「HTTPリファラー」
   - ウェブサイトの制限:
     - `http://localhost:5000/*`
     - `https://あなたのドメイン.onrender.com/*`
   - APIの制限: 「キーを制限」
     - Google Picker API
     - Google Drive API
4. 「保存」

## 7. 環境変数の設定

### ローカル環境（Windows）

1. システム環境変数に追加:
   ```
   GOOGLE_API_KEY=あなたのAPIキー
   GOOGLE_CLIENT_ID=あなたのクライアントID.apps.googleusercontent.com
   ```

2. または、start_server.batを編集:
   ```bat
   set GOOGLE_API_KEY=あなたのAPIキー
   set GOOGLE_CLIENT_ID=あなたのクライアントID.apps.googleusercontent.com
   python app.py
   ```

### Render（本番環境）

1. Renderダッシュボードでサービスを選択
2. 「Environment」タブを開く
3. 以下の環境変数を追加:

| Key | Value |
|-----|-------|
| `GOOGLE_API_KEY` | 手順6で作成したAPIキー |
| `GOOGLE_CLIENT_ID` | 手順5で作成したクライアントID |

4. 「Save Changes」をクリック

## 8. 動作確認

1. サーバーを再起動
2. 商品登録画面を開く
3. 「Googleドライブから選択」ボタンが表示されることを確認
4. ボタンをクリックしてGoogleアカウントで認証
5. ドライブから画像を選択できることを確認

## トラブルシューティング

### ボタンが表示されない

- 環境変数が正しく設定されているか確認
- サーバーを再起動したか確認
- ブラウザのコンソールでエラーを確認

### 「このアプリは確認されていません」と表示される

- 開発中は「詳細」→「（安全ではないページ）に移動」で続行可能
- 本番運用時はGoogleの審査を通す必要があります

### 画像が選択できない

- Google Picker APIとGoogle Drive APIが有効になっているか確認
- OAuth同意画面でスコープが正しく設定されているか確認
- APIキーの制限が正しいか確認

## セキュリティに関する注意

1. **APIキーを公開しない**: GitHubなどに環境変数を含むファイルをプッシュしない
2. **APIキーを制限する**: 本番環境では必ずHTTPリファラー制限を設定
3. **OAuth同意画面の審査**: 多数のユーザーが使用する場合はGoogleの審査を受ける

## 費用

- Google Picker API: **無料**
- Google Drive API: **無料**（1日あたり10億クエリまで）

通常の使用では費用は発生しません。
