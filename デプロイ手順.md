# 物販管理システム デプロイ手順

## ローカルで起動する方法

1. `★物販管理ツール` フォルダ内の `物販管理ツールWeb版.bat` をダブルクリック
2. ブラウザで `http://localhost:5000` を開く
3. 終了時は `物販管理ツールWeb版を終了.bat` を実行

## デフォルトログイン情報

- **ユーザー名**: `admin`
- **パスワード**: `admin123`
- **権限**: 管理者

## クラウドデプロイ (Render.com)

### 1. GitHubにアップロード

1. GitHubで新しいリポジトリを作成
2. `merchandise_web` フォルダの内容をアップロード

### 2. Render.comでデプロイ

1. [render.com](https://render.com) にログイン
2. 「New +」→「Web Service」を選択
3. GitHubリポジトリを接続
4. 以下を設定:
   - **Name**: merchandise-management
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app --bind 0.0.0.0:$PORT`

### 3. データベース設定 (Render PostgreSQL)

1. Renderダッシュボードで「New +」→「PostgreSQL」を選択
2. 以下を設定:
   - **Name**: merchandise-db
   - **Database**: merchandise
   - **User**: merchandise_user
   - **Region**: Singapore（日本に近い）
   - **Plan**: Starter ($7/月)
3. 「Create Database」をクリック
4. 作成完了後、「Internal Database URL」をコピー

### 4. Web ServiceとPostgreSQLの接続

1. Web Serviceの設定画面を開く
2. 「Environment」タブを選択
3. 「Add Environment Variable」をクリック
4. 以下を設定:
   - **Key**: `DATABASE_URL`
   - **Value**: 手順3でコピーした「Internal Database URL」
5. 「Save Changes」をクリック

### 5. 環境変数

| 変数名 | 説明 | 設定方法 |
|--------|------|----------|
| `DATABASE_URL` | PostgreSQL接続文字列 | Render PostgreSQLからコピー |
| `SECRET_KEY` | セッション暗号化キー | 自動生成（Generate） |

> 💡 **Render完結型**: 外部サービス（Supabase等）は不要です

## 機能一覧

### ユーザー機能
- ✅ ログイン / 新規登録
- ✅ プロフィール編集
- ✅ 商品の登録・編集・削除
- ✅ 商品のフィルター・検索
- ✅ CSV出力
- ✅ 顧客管理

### 管理者機能
- ✅ 全ユーザーの統計表示
- ✅ ユーザー管理（権限変更・削除）
- ✅ 各ユーザーの商品一覧表示
- ✅ マーケティング分析
  - 月別売上推移
  - 販売先別統計
  - 支払方法別統計

## トラブルシューティング

### 起動しない場合
```bash
pip install -r requirements.txt
```

### データベースエラー
- `merchandise.db` を削除して再起動
- 環境変数 `DATABASE_URL` を確認


